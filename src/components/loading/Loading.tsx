import React from 'react';
import ReactLoading from 'react-loading';

const Loading = (): JSX.Element => (
  <div>
    <ReactLoading type="bars" color="#ffe81f" height="40%" width="40%" />
  </div>
);

export default Loading;
